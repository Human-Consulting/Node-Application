import { Box, styled } from "@mui/material";

export const NavTask = styled(Box)({
    height: '50px',
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
});