import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const BackgroundMain = styled(Box)({
    width: '400px',
    height: '300px',
    background: "red",
    display: 'flex',
    flexDirection: 'column'

});