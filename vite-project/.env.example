# Base URL of the HumanCore-Application (Spring Boot) REST API, no trailing slash.
# Also used to derive the chat WebSocket endpoint (${VITE_ENDERECO_API}/websocket) --
# the WebSocket relay is now embedded in HumanCore-Application, not a separate service.
VITE_ENDERECO_API=http://localhost:8080
